version: '3.8'

services:
  adaptive-rag-mcp:
    build: .
    container_name: adaptive_rag_mcp
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
